<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.ahmedabdelsttar.sec_final">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".splash_screen">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".MainActivity"

            android:label="امتحانات" />

        <activity android:name=".bokolit"

            android:label="امتحانات البوكليت" />

        <activity android:name=".courses"

            android:label="المواد" />

        <activity android:name=".proofExam"

            android:label="امتحانات دليل التقويم" />

        <activity android:name=".pastExam"

            android:label="امتحانات السنوات السابقة" />

        <activity android:name=".sudanExam"

            android:label="اامتحانات السودان" />

        <activity android:name=".expe_Exam"
            android:label="امتحانات تجريبية للوزارة"/>

        <activity android:name=".sudan_2017"
            android:label="السودان 2017"/>

        <activity android:name=".proof_instead"
            android:label="زوروا صفحتنا"/>




    </application>

</manifest>